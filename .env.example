# Suture itself does not require environment variables to run.
#
# The variables below appear in files under examples/broken-projects/ —
# they are intentional test fixtures demonstrating ENV002/ENV004 detection.
#
DATABASE_URL=
REDIS_URL=
SECRET_KEY=
